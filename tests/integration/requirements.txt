# Test-runner dependencies for the Docker/Podman integration suite
# (tests/integration/). These run on the *host*, driving Home Assistant and the
# mock 2N server over HTTP/WebSocket — they are separate from the unit-test
# stack in ../../requirements_test.txt, which pulls in
# pytest-homeassistant-custom-component and its own pinned pytest.
#
# Pin exactly. These were installed unpinned until 2026-08, which let a CI
# toolchain change land silently and hang the integration job for days.
# Bump deliberately, in a commit of its own, so a break is bisectable.
aiohttp==3.14.3
pytest==9.1.1
pytest-asyncio==1.4.0
websockets==17.0.1
