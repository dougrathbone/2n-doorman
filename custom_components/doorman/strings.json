{
  "config": {
    "step": {
      "user": {
        "title": "Connect to your 2N intercom",
        "description": "Enter the connection details for your 2N device. Ensure the HTTP API is enabled and a user with Directory access is configured under Services → HTTP API on the device.",
        "data": {
          "host": "Host / IP address",
          "username": "API username",
          "password": "API password",
          "use_ssl": "Use HTTPS",
          "verify_ssl": "Verify SSL certificate"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate Doorman",
        "description": "The credentials for **{host}** are no longer valid. Enter new credentials to restore the connection.",
        "data": {
          "username": "API username",
          "password": "API password"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to the device. Check the host and that the HTTP API is enabled.",
      "invalid_auth": "Invalid username or password.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "already_configured": "This device is already configured.",
      "reauth_successful": "Re-authentication was successful."
    }
  },
  "exceptions": {
    "unknown_device": {
      "message": "Unknown Doorman device: {device}"
    },
    "no_devices_configured": {
      "message": "No Doorman devices are configured."
    },
    "multiple_devices_no_selector": {
      "message": "Multiple Doorman devices configured — specify the 'device' field (config entry ID) to target one."
    }
  },
  "issues": {
    "no_write_permission": {
      "title": "Doorman: directory write unavailable on {device_name}",
      "description": "The API user `{username}` cannot write to the 2N device directory on **{device_name}**. Create, edit, and delete operations are disabled.\n\n**To fix:** In the 2N web interface go to **Services → HTTP API**, find the API user, and enable **System API** with **Control** access. Access log, switch control, and user viewing will continue to work in the meantime."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Doorman options",
        "description": "Adjust how often Doorman polls the device for user and switch updates, and configure doorbell notifications for this device.",
        "data": {
          "poll_interval": "Poll interval (seconds)",
          "doorbell_key_code": "Doorbell key code",
          "doorbell_targets": "Doorbell notification targets"
        },
        "data_description": {
          "doorbell_key_code": "Value 2N reports in KeyPressed.params.key when the quick-dial/call button is pressed. Defaults to %1 for the Verso; use %2, %3, ... for additional buttons.",
          "doorbell_targets": "Devices to notify when the doorbell button is pressed. Nothing is sent if no targets are selected."
        }
      }
    }
  },
  "services": {
    "create_user": {
      "name": "Create user",
      "description": "Add a new user to the 2N directory."
    },
    "update_user": {
      "name": "Update user",
      "description": "Update an existing user's credentials."
    },
    "delete_user": {
      "name": "Delete user",
      "description": "Remove a user from the 2N directory."
    },
    "grant_access": {
      "name": "Grant access",
      "description": "Open an access point immediately, bypassing credential checks."
    },
    "hangup_calls": {
      "name": "Hang up calls",
      "description": "Hang up all active call sessions on the 2N device."
    }
  }
}
